<Border
    x:Class="SLAM.Views.Controls.Playback"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SLAM.Views.Controls"
    Padding="4">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="0,0,4,0"/>
                    <Setter Property="Padding" Value="10,2"/>
                </Style>
            </Grid.Resources>
            <Button
                Grid.Column="0"
                Content="B"/>
            <Button
                Grid.Column="1"
                Content="PLAY"/>
            <Button
                Grid.Column="2"
                Content="F"
                Margin="0"/>
        </Grid>
    </StackPanel>
</Border>